module.exports = {
  plugins: {
    // Tailwind v4: the PostCSS plugin moved to its own package and bundles
    // autoprefixer, so no separate autoprefixer entry is needed.
    "@tailwindcss/postcss": {},
  },
};
