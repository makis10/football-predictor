<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.football-predictor.cloudflared</string>

    <!-- Runs the named cloudflared tunnel (creds in ~/.cloudflared/) forwarding
         aitipster.net → localhost:3000. Binary is invoked DIRECTLY — a wrapper
         script inside ~/Documents gets blocked by macOS TCC ("Operation not
         permitted") when launched from launchd. See launchd/CLOUDFLARED_SETUP.md. -->
    <key>ProgramArguments</key>
    <array>
        <string>/opt/homebrew/bin/cloudflared</string>
        <string>tunnel</string>
        <string>--no-autoupdate</string>
        <string>run</string>
        <string>--url</string>
        <string>http://localhost:3000</string>
        <string>aitipster</string>
    </array>

    <!-- Start at login, restart if it crashes -->
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>

    <key>StandardOutPath</key>
    <string>__LOG_DIR__/tunnel-stdout.log</string>
    <key>StandardErrorPath</key>
    <string>__LOG_DIR__/tunnel-stderr.log</string>

    <!-- Minimum 10 s between restarts to avoid spinning -->
    <key>ThrottleInterval</key>
    <integer>10</integer>
</dict>
</plist>
