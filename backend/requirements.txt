# ML / Data
pandas==3.0.3
numpy==2.5.0
scikit-learn==1.9.0
xgboost==3.3.0
lightgbm==4.6.0
catboost==1.2.10
requests==2.34.2
# understat (xG for top-5 leagues) is installed --no-deps in the Dockerfile:
# its setup wrongly pins pytest==7.2.0 / aiohttp==3.8.3 as RUNTIME deps, which
# conflicts with our pytest 8.x and modern charset-normalizer. We provide its
# real runtime deps (aiohttp + beautifulsoup4) here instead.
aiohttp>=3.9,<4          # understat HTTP client
beautifulsoup4>=4.12     # understat HTML parsing

# Groq (Llama-3.3-70B) — analysis + chatbot
groq==1.5.0

# API
fastapi==0.138.1
uvicorn[standard]==0.49.0
sqlalchemy==2.0.51
alembic==1.18.5
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
redis==8.0.1

# Auth
bcrypt==5.0.0

# Error tracking (optional — inert unless SENTRY_DSN is set)
sentry-sdk[fastapi]==2.63.0

# Tests
pytest==9.1.1
httpx==0.28.1   # FastAPI TestClient transport
